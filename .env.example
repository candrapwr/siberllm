# Copy this file to .env and fill in your token.
#   cp .env.example .env
#
# Create a classic PAT at https://github.com/settings/tokens (scope: "repo").
# NEVER commit .env — it is gitignored.

GITHUB_TOKEN=ghp_your_token_here
